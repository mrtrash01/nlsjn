const discord = require("discord.js");

module.exports.run = async(bot, message, args) => {

   return message.reply("This command doesn't work, we will fix it as soon as possible");
}
module.exports.help = {
    name: "unmute"
}